// config.js — single place to point the front-end at your running backend.
// Local dev default matches backend/.env.example's PORT=4000.
window.OBIT_API_BASE = window.OBIT_API_BASE || 'http://localhost:4000';
